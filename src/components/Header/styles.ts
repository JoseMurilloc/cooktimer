import { styled } from "../../styles";

export const ContainerHeader = styled('header', {
  width: '100vw',
  backgroundColor: '$primary_400',

  h1: {
    fontWeight: '$bold',
    fontSize: '$5xl'
  }
})
